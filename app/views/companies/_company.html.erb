<tr id="company_<%=company.id %>">
	<td><%= company.name %></td>
	<td><%= company.address %></td>
	<td><%= link_to t('edit'), edit_company_path(company), remote: true, :class => 'btn btn-sm btn-info' %>
    </td>
	<td><%= link_to t('delete'), company, remote: true, :method => :delete, data: {confirm:  " Удалить :#{company.name}"}, :class => 'btn btn-sm btn-danger' %>
 </td>
</tr>