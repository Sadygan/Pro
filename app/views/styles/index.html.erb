<div class="container"> 
<p id="notice"><%= notice %></p>

<h2><%= t('styles') %></h2>
  <div class="well">
    <%= link_to t('product.new_product'), new_style_path, remote: true, class: "btn btn-primary" %>
  </div>
  <div class="new-style"></div>

<table class="table table-bordered table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody id="styles">
      <%= render @styles %>
  </tbody>
</table>
<%= render 'dialog' %>
</div>