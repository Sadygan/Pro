<% unless @size_image.nil? %>
	<%= link_to image_tag(@size_image.img.url, value: @size_image.id), "#", class: "invoker" %>
<% else %>
	<%= link_to image_tag('UploadDark.png'), "#", {class: "invoker", id: "upload_dark"} %>
<% end %>