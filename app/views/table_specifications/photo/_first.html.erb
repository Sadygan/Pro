<% unless @photo.nil? %>
	<%= link_to image_tag(@photo.img.url), "#", class: "invoker" %>
<% else %>
	<%= link_to image_tag('UploadDark.png'), "#", {class: "invoker", id: "upload_dark"} %> 
<% end %>
