<p id="notice"><%= notice %></p>

<h1>Listing Size Images</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @size_images.each do |size_image| %>
      <tr>
        <%= image_tag size_image.img.url %> 
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Size image', new_product_size_image_path %>
